import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as NavBarStories from './NavBar.stories';

<Meta of={NavBarStories} />

# NavBar

Site header — wordmark left, three nav links right. Present on every page. Transparent: sits directly on the page background with only a 1px bottom rule. Wire `activePath` to the router pathname to mark the active link.

The `BM_` wordmark with blinking underscore is a terminal cursor convention — do not modify the wordmark or remove the blink.

The Retro/Futuristic switch at the right edge is a separate documented component — see [ThemeToggle](/docs/components-themetoggle--docs) for its own states and guidance. NavBar's `Futuristic V2` story below renders it live.

## States

### Work active (homepage)

<Canvas of={NavBarStories.WorkActive} />

### About active

<Canvas of={NavBarStories.AboutActive} />

### No active route (404 / loading)

<Canvas of={NavBarStories.Default} />

## Do's

- **Wire `activePath` to the current router pathname** on every page.
- **Use the three fixed links:** Work `/work`, About `/about`, Contact `/contact`.

## Don'ts

- **Don't leave `activePath` undefined in production.** All links render at disabled-text when no route matches.
- **Don't add nav links or dropdown menus.** Three links fit at 390px without collapsing.
- **Don't add a mobile hamburger.** The nav is intentionally always visible.

## AI guidance

Site header — present on every page. Three fixed nav links: Work, About, Contact. Always wire `activePath` to the current router pathname.

**Content rules**
- Nav links are fixed: Work (`/work`), About (`/about`), Contact (`/contact`). Do not add custom links.
- The `BM_` wordmark with blinking underscore is the identity mark — do not modify it.
- Active link gets green-accent color + `aria-current="page"`. No underline, no fill.
- Wire `activePath` to the router pathname: `'/work'` on the homepage, `'/about'` on the About page, `'/contact'` on the Contact page.

**Avoid**
- Don't add a mobile hamburger — three links fit at 390px without collapsing.
- Don't leave `activePath` undefined in production — all links render at disabled-text when no route matches.
- Don't add extra nav links or dropdown menus.

## Props

<ArgTypes of={NavBarStories} />
